{
  "name": "alma-advisor",
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next"
    }
  ],
  "env": {
    "DATABASE_URL": "@database_url",
    "OPENAI_API_KEY": "@openai_api_key",
    "LLM_PROVIDER": "openai",
    "UPLOAD_DIR": "./uploads",
    "MAX_FILE_SIZE": "50000000"
  },
  "build": {
    "env": {
      "DATABASE_URL": "@database_url",
      "OPENAI_API_KEY": "@openai_api_key"
    }
  }
}
